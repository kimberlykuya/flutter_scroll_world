name: scroll_world_workspace
description: Workspace for the scroll_world package and demonstration app.
publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  crypto: ^3.0.6

workspace:
  - packages/scroll_world
  - example
