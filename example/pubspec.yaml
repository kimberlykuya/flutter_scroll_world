name: scroll_world_example
description: Kenya in Motion demonstration for the scroll_world package.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  scroll_world: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/posters/
    - assets/videos/
