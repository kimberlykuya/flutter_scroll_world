# Padlo real-time scene

`padlo-pilot.glb` is exported from `tools/blender/generate_padlo_world.py`
with Blender 5.2. It is a source GLB for the Flutter Scene web pilot. The
runtime pauses the authored animation and seeks it from scroll progress; it is
not a video texture.
